import type { Metadata } from "next";
import HomePage from "@/app/HomePage";
import { workspacePageMetadata } from "@/lib/siteMetadata";

export const metadata: Metadata = workspacePageMetadata;

export default HomePage;
