import { Vector3 } from "three";

/** Pedagogical column: shared center (world space), radius on XZ, height along Y. */
export const OBSTACLE_CENTER = new Vector3(1.85, 0, -1.15);
export const OBSTACLE_RADIUS = 0.48;
export const OBSTACLE_HEIGHT = 3.2;
